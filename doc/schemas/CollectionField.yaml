type: object
properties:
  name:
    type: string
  data_type:
    $ref: "./CollectionDataType.yaml"
  max_length:
    type: integer
    minimum: 1
    maximum: 65535
    description: The maximum length of the field. It's mandatory for VARCHAR data type.
  is_primary:
    type: boolean
